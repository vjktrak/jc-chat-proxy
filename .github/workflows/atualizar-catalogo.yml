name: Atualizar catalogo diariamente

on:
  schedule:
    - cron: "0 18 * * *"
  workflow_dispatch:

jobs:
  redeploy:
    runs-on: ubuntu-latest
    steps:
      - name: Acionar redeploy no Vercel
        run: curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}" --fail --silent --show-error
